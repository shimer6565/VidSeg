# VidSeg
a web application developed using Flask framework, HTML and CSS that allows users to enter queries and segments the video using the YOLOv7 model. It also summarizes the video, generates questions and answers based on the segmented video and provides a dashboard which helps the users, giving a quick glance on the desired content.
